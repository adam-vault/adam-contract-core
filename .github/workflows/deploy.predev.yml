name: Deploy Pre-Dev
on:
  push:
    tags:    
      - 'v[0-9].[0-9]+.[0-9]+\-predev'

jobs:
  deploy-contracts:
    name: Deploy
    uses: ./.github/workflows/reusable-deploy-contracts.yml
    with:
      GIT_REF: ${{ github.ref_name }}
      CONTRACT_DEPLOY_NETWORK: rinkeby
    secrets:
      SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
      RINKEBY_URL: ${{ secrets.RINKEBY_URL }}
      KOVAN_URL: ''
      PRIVATE_KEY: ${{ secrets.PRIVATE_KEY }}

  sync-backend:
    name: Sync backend Integration
    needs: deploy-contracts
    uses: ./.github/workflows/reusable-sync-backend.yml
    with:
      GIT_REF: ${{ github.ref_name }}
      DEPLOY_ENV: dev
    secrets:
      GH_PAT: ${{ secrets.GH_PAT }}